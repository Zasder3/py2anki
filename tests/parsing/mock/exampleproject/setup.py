"""This file exists so we can ensure that the project contains valid python code."""

from setuptools import find_packages, setup

setup(
    name="exampleproject",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[],
    python_requires=">=3.6",
)
